# Execute the following to run a single experiment with the parameters as set in the file
python3 example.py

# Check that everything is stored the way it should be:
cd data/<experiment number>/
cp ../../analyze.py .
python3 analyze.py

